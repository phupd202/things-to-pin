{
  "name": "things-to-pin",
  "private": true,
  "description": "Bảng ghim chung của phòng",
  "scripts": {
    "build": "node scripts/build.js",
    "preview": "npm run build && npx --yes serve dist -p 8080"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "esbuild": "^0.25.9"
  }
}
