// Cấu hình Supabase.
// Điền URL và anon key của project Supabase (Settings → API).
// Anon key là khóa public phía client, an toàn khi để trong frontend.
// Nếu để trống, app chạy ở chế độ cục bộ (localStorage) — dữ liệu chỉ lưu trên trình duyệt này.
window.APP_CONFIG = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: ''
};
